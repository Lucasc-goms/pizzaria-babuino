/* ==========================================================================
   PIZZARIA DO BABUÍNO — Style System
   Estética: Pizzaria Napolitana Artesanal — quente, rústica, editorial
   ========================================================================== */

@import url("https://fonts.googleapis.com/css2?family=Playfair+Display:ital,wght@0,700;0,900;1,700&family=Lora:ital,wght@0,400;0,600;1,400&family=DM+Sans:wght@400;500;600&display=swap");

/* --------------------------------------------------------------------------
   TOKENS
   -------------------------------------------------------------------------- */
:root {
  --vermelho: #c1330a;
  --laranja: #e8621a;
  --laranja-mel: #f5913a;
  --creme: #fdf6ed;
  --creme-escuro: #f2e8d8;
  --marrom: #4a2c0a;
  --marrom-suave: #7a4e28;
  --cinza-texto: #3d2b1f;
  --cinza-leve: #8a7468;
  --verde-dica: #2d6a4f;
  --fundo-dica: #d8f3dc;

  --ff-display: "Playfair Display", Georgia, serif;
  --ff-body: "Lora", Georgia, serif;
  --ff-ui: "DM Sans", sans-serif;

  --radius-card: 14px;
  --sombra: 0 4px 20px rgba(74, 44, 10, 0.1);
  --sombra-hover: 0 8px 30px rgba(74, 44, 10, 0.18);
}

/* --------------------------------------------------------------------------
   RESET & BASE
   -------------------------------------------------------------------------- */
*,
*::before,
*::after {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}

html {
  scroll-behavior: smooth;
}

body {
  font-family: var(--ff-body);
  background: var(--creme);
  color: var(--cinza-texto);
  line-height: 1.7;
  -webkit-font-smoothing: antialiased;
}

img {
  max-width: 100%;
  height: auto;
  border-radius: 10px;
  object-fit: cover;
  display: block;
}

/* --------------------------------------------------------------------------
   HEADER
   -------------------------------------------------------------------------- */
header {
  background: var(--marrom);
  color: var(--creme);
  text-align: center;
  padding: 60px 24px 50px;
  position: relative;
  overflow: hidden;
}

/* Textura pontilhada sutil */
header::before {
  content: "";
  position: absolute;
  inset: 0;
  background-image: radial-gradient(
    circle,
    rgba(245, 145, 58, 0.18) 1px,
    transparent 1px
  );
  background-size: 22px 22px;
  pointer-events: none;
}

header h1 {
  font-family: var(--ff-display);
  font-size: clamp(2.2rem, 6vw, 3.8rem);
  font-weight: 900;
  letter-spacing: -0.01em;
  line-height: 1.1;
  position: relative;
}

header h1 span {
  display: block;
  font-size: 0.42em;
  font-weight: 400;
  font-style: italic;
  color: var(--laranja-mel);
  letter-spacing: 0.04em;
  margin-top: 4px;
}

header > p {
  font-family: var(--ff-ui);
  font-size: 1rem;
  color: rgba(253, 246, 237, 0.72);
  margin-top: 14px;
  position: relative;
  letter-spacing: 0.03em;
}

/* Ornamento decorativo */
header::after {
  content: "— ◆ —";
  display: block;
  color: var(--laranja-mel);
  font-size: 0.85rem;
  margin-top: 18px;
  letter-spacing: 0.2em;
  position: relative;
}

/* --------------------------------------------------------------------------
   NAV
   -------------------------------------------------------------------------- */
nav {
  background: var(--vermelho);
  text-align: center;
  padding: 0 16px;
  position: sticky;
  top: 0;
  z-index: 1000;
  box-shadow: 0 2px 12px rgba(193, 51, 10, 0.3);
}

nav a {
  font-family: var(--ff-ui);
  font-weight: 600;
  font-size: 0.82rem;
  letter-spacing: 0.08em;
  text-transform: uppercase;
  color: rgba(253, 246, 237, 0.9);
  text-decoration: none;
  display: inline-block;
  padding: 15px 14px;
  transition: color 0.2s;
}

nav a:hover {
  color: var(--laranja-mel);
}

/* --------------------------------------------------------------------------
   MAIN CONTAINER
   -------------------------------------------------------------------------- */
main {
  max-width: 960px;
  margin: 0 auto;
  padding: 50px 24px 60px;
}

section {
  margin-bottom: 64px;
}

/* --------------------------------------------------------------------------
   HEADINGS
   -------------------------------------------------------------------------- */
h2 {
  font-family: var(--ff-display);
  font-size: clamp(1.6rem, 4vw, 2.2rem);
  color: var(--marrom);
  font-weight: 700;
}

h3 {
  font-family: var(--ff-display);
  font-size: 1.35rem;
  color: var(--vermelho);
  font-weight: 700;
}

/* --------------------------------------------------------------------------
   BOAS-VINDAS
   -------------------------------------------------------------------------- */
#boas-vindas {
  text-align: center;
  padding: 42px 32px;
  background: var(--creme-escuro);
  border-radius: var(--radius-card);
  border: 1px solid rgba(74, 44, 10, 0.1);
}

#boas-vindas h2 {
  font-size: clamp(1.8rem, 4vw, 2.5rem);
  margin-bottom: 12px;
}

#boas-vindas p {
  font-size: 1.05rem;
  color: var(--marrom-suave);
  font-style: italic;
}

/* --------------------------------------------------------------------------
   INSTITUCIONAL
   -------------------------------------------------------------------------- */
.institucional-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
  gap: 20px;
  list-style: none;
}

.institucional-grid li {
  background: white;
  padding: 28px 22px;
  border-radius: var(--radius-card);
  border-top: 4px solid var(--laranja);
  box-shadow: var(--sombra);
}

.institucional-grid li h2 {
  font-size: 1.1rem;
  color: var(--vermelho);
  margin-bottom: 8px;
  text-transform: uppercase;
  letter-spacing: 0.06em;
  font-family: var(--ff-ui);
  font-weight: 600;
}

.institucional-grid li p {
  font-size: 0.92rem;
  color: var(--cinza-leve);
  line-height: 1.6;
}

/* --------------------------------------------------------------------------
   SOBRE / GALERIA
   -------------------------------------------------------------------------- */
#sobre h2 {
  margin-bottom: 14px;
}
#sobre p {
  color: var(--cinza-leve);
  margin-bottom: 10px;
}

.galeria-fotos {
  display: flex;
  gap: 16px;
  flex-wrap: wrap;
  margin-top: 24px;
}

.galeria-fotos img {
  flex: 1 1 260px;
  height: 200px;
  border-radius: 10px;
  box-shadow: var(--sombra);
  transition: transform 0.3s;
}
.galeria-fotos img:hover {
  transform: scale(1.02);
}

/* --------------------------------------------------------------------------
   CARDÁPIO
   -------------------------------------------------------------------------- */
#cardapio h2 {
  text-align: center;
  font-size: clamp(1.8rem, 4vw, 2.4rem);
  margin-bottom: 4px;
}

.subtitulo-cardapio {
  text-align: center;
  font-style: italic;
  color: var(--cinza-leve);
  font-size: 0.97rem;
  margin-bottom: 32px;
  font-family: var(--ff-body);
}

.cardapio-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
  gap: 18px;
  list-style: none;
}

.cardapio-grid li {
  background: white;
  padding: 20px 18px;
  border-radius: var(--radius-card);
  border-left: 5px solid var(--laranja);
  box-shadow: var(--sombra);
  transition:
    transform 0.25s,
    box-shadow 0.25s;
}

.cardapio-grid li:hover {
  transform: translateY(-4px);
  box-shadow: var(--sombra-hover);
}

.sabor {
  display: block;
  font-family: var(--ff-display);
  font-weight: 700;
  font-style: italic;
  color: var(--vermelho);
  font-size: 1.15rem;
  margin-bottom: 6px;
}

.cardapio-grid li p {
  font-size: 0.88rem;
  color: var(--cinza-leve);
  line-height: 1.55;
}

/* --------------------------------------------------------------------------
   CHAMADA PARA RECEITA
   -------------------------------------------------------------------------- */
.chamada-receita {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 18px;
  padding: 40px 24px;
  background: var(--marrom);
  border-radius: var(--radius-card);
  flex-wrap: wrap;
}

.chamada-receita span {
  font-family: var(--ff-display);
  font-size: 1.25rem;
  font-style: italic;
  color: var(--creme);
}

/* --------------------------------------------------------------------------
   BOTÃO
   -------------------------------------------------------------------------- */
.botao-link {
  font-family: var(--ff-ui);
  font-weight: 600;
  font-size: 0.9rem;
  letter-spacing: 0.07em;
  text-transform: uppercase;
  background: var(--laranja);
  color: white;
  text-decoration: none;
  padding: 13px 28px;
  border-radius: 50px;
  display: inline-block;
  transition:
    background 0.2s,
    transform 0.2s;
  box-shadow: 0 4px 14px rgba(232, 98, 26, 0.35);
}

.botao-link:hover {
  background: var(--vermelho);
  transform: scale(1.04);
}

/* --------------------------------------------------------------------------
   FORMULÁRIO
   -------------------------------------------------------------------------- */
#contato h2 {
  margin-bottom: 20px;
}

form {
  display: flex;
  flex-direction: column;
  gap: 4px;
}

form label {
  font-family: var(--ff-ui);
  font-size: 0.82rem;
  font-weight: 600;
  letter-spacing: 0.05em;
  text-transform: uppercase;
  color: var(--marrom-suave);
  margin-bottom: 2px;
  margin-top: 10px;
}

form input,
form textarea {
  padding: 12px 14px;
  border: 1.5px solid var(--creme-escuro);
  border-radius: 8px;
  font-family: var(--ff-body);
  font-size: 0.95rem;
  background: white;
  color: var(--cinza-texto);
  transition: border-color 0.2s;
  margin-bottom: 4px;
}

form input:focus,
form textarea:focus {
  outline: none;
  border-color: var(--laranja);
}

form textarea {
  resize: vertical;
  min-height: 120px;
}

button[type="submit"] {
  font-family: var(--ff-ui);
  font-weight: 600;
  letter-spacing: 0.07em;
  text-transform: uppercase;
  background: var(--vermelho);
  color: white;
  border: none;
  padding: 14px;
  cursor: pointer;
  border-radius: 8px;
  font-size: 0.9rem;
  margin-top: 10px;
  transition:
    background 0.2s,
    transform 0.15s;
}

button[type="submit"]:hover {
  background: var(--marrom);
  transform: scale(1.01);
}

/* --------------------------------------------------------------------------
   FOOTER
   -------------------------------------------------------------------------- */
footer {
  text-align: center;
  padding: 28px 20px;
  background: var(--marrom);
  color: rgba(253, 246, 237, 0.6);
  font-family: var(--ff-ui);
  font-size: 0.82rem;
  letter-spacing: 0.04em;
}

/* ==========================================================================
   PÁGINA DE RECEITAS
   ========================================================================== */

.topo-receitas {
  text-align: center;
  margin-bottom: 40px;
  padding-bottom: 28px;
  border-bottom: 2px solid var(--creme-escuro);
}

.topo-receitas h2 {
  font-size: clamp(1.8rem, 4vw, 2.6rem);
  color: var(--marrom);
  margin-bottom: 8px;
}

.topo-receitas p {
  font-style: italic;
  color: var(--cinza-leve);
  font-size: 1rem;
}

/* Grid topo (Planejamento + Ingredientes) */
.Grid-receita-topo {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
  gap: 20px;
  margin-bottom: 24px;
}

/* Bloco base */
.bloco-receita {
  background: white;
  padding: 28px;
  border-radius: var(--radius-card);
  border: 1px solid var(--creme-escuro);
  box-shadow: var(--sombra);
}

.bloco-receita h3 {
  margin-top: 0;
  font-size: 1.2rem;
  border-bottom: 2px solid var(--laranja);
  padding-bottom: 10px;
  margin-bottom: 18px;
}

/* Planejamento */
.destaque-tempo {
  font-family: var(--ff-display);
  font-size: 1.05rem;
  color: var(--laranja);
  font-style: italic;
}

.alerta {
  background: #fff3cd;
  border-left: 3px solid #e8a020;
  padding: 10px 14px;
  border-radius: 6px;
  font-size: 0.85rem;
  color: #7a5000;
  margin-top: 10px;
  font-family: var(--ff-ui);
}

/* Ingredientes */
.sub-ingrediente {
  color: var(--cinza-leve);
  font-size: 0.88rem;
  margin-top: -10px;
  font-style: italic;
}

#bloco-ingredientes ul {
  display: block;
  padding-left: 22px;
  list-style: disc;
}

#bloco-ingredientes ul li {
  background: none !important;
  padding: 6px 0 !important;
  border-radius: 0 !important;
  box-shadow: none !important;
  border-left: none !important;
  list-style: disc !important;
  font-size: 0.95rem;
  color: var(--cinza-texto);
  transform: none !important;
}

/* Preparo — linha do tempo */
#bloco-preparo {
  margin-bottom: 40px;
}

.passo {
  margin-bottom: 22px;
  border-bottom: 1px dashed var(--creme-escuro);
  padding-bottom: 18px;
}

.passo h4,
.passo-final h4 {
  font-family: var(--ff-ui);
  font-weight: 600;
  font-size: 0.88rem;
  text-transform: uppercase;
  letter-spacing: 0.07em;
  color: var(--marrom-suave);
  margin-bottom: 6px;
}

.passo p,
.passo-final p {
  font-size: 0.95rem;
  line-height: 1.65;
  color: var(--cinza-texto);
}

.dica-interna {
  display: inline-block;
  font-size: 0.82rem;
  color: var(--verde-dica);
  background: var(--fundo-dica);
  padding: 6px 12px;
  border-radius: 4px;
  margin-top: 6px;
  font-family: var(--ff-ui);
}

.passo-final {
  background: var(--creme-escuro);
  padding: 18px;
  border-radius: 8px;
  border-left: 4px solid var(--vermelho);
}

/* Montagem */
.secao-montagem {
  margin-top: 30px;
  border-top: 2px solid var(--creme-escuro);
  padding-top: 36px;
}

.secao-montagem > h3 {
  text-align: center;
  font-size: 1.55rem;
  color: var(--marrom);
  margin-bottom: 4px;
}

.sub-montagem {
  text-align: center;
  color: var(--cinza-leve);
  font-style: italic;
  margin-bottom: 28px;
  font-size: 0.95rem;
}

.grid-montagem {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(230px, 1fr));
  gap: 16px;
}

.mini-card-recheio {
  background: white;
  padding: 18px;
  border-radius: var(--radius-card);
  border: 1px solid var(--creme-escuro);
  border-top: 4px solid var(--laranja);
  box-shadow: var(--sombra);
  transition: transform 0.2s;
}

.mini-card-recheio:hover {
  transform: translateY(-3px);
}

.mini-card-recheio-doce {
  background: #fff8f5;
  padding: 18px;
  border-radius: var(--radius-card);
  border: 1px solid #f5ceba;
  border-top: 4px solid #e87080;
  box-shadow: var(--sombra);
  transition: transform 0.2s;
}

.mini-card-recheio-doce:hover {
  transform: translateY(-3px);
}

.mini-card-recheio h4,
.mini-card-recheio-doce h4 {
  font-family: var(--ff-display);
  font-style: italic;
  font-size: 1rem;
  color: var(--marrom);
  margin-bottom: 8px;
}

.mini-card-recheio p,
.mini-card-recheio-doce p {
  font-size: 0.86rem;
  color: var(--cinza-leve);
  line-height: 1.5;
}

/* Botão de voltar centralizado */
.bloco-voltar {
  text-align: center;
  margin: 44px 0 10px;
}

/* --------------------------------------------------------------------------
   RESPONSIVO
   -------------------------------------------------------------------------- */
@media (max-width: 600px) {
  main {
    padding: 32px 16px 48px;
  }
  nav a {
    font-size: 0.75rem;
    padding: 12px 8px;
  }
  .chamada-receita {
    flex-direction: column;
    text-align: center;
  }
}
